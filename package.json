{
  "name": "redox-interview",
  "version": "0.0.0",
  "description": "RedoxInterview",
  "main": "app.js",
  "author": {
    "name": ""
  },
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc -w",
    "clean": "tsc --build --clean",
    "start": "tsc --build && node app.js"
  },
  "devDependencies": {
    "@types/node": "^14.14.7",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "node-fetch": "^2.6.7"
  }
}
